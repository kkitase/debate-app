# ── Gemini ────────────────────────────────────────────────
# Required for Gemini models. Get yours at https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY="your_gemini_api_key"
GEMINI_API_KEY="your_gemini_api_key"

# ── Claude via Vertex AI ──────────────────────────────────
# GCP project ID that has Vertex AI / Claude access enabled in Model Garden.
VERTEX_PROJECT_ID="your_vertex_gcp_project_id"

# Vertex AI region. Claude models are available in: us-east5, europe-west1, asia-southeast1
VERTEX_REGION="us-east5"

# Firebase project ID for Auth token verification.
# Only needed if your Firebase project differs from VERTEX_PROJECT_ID.
# If omitted, VERTEX_PROJECT_ID is used as fallback.
FIREBASE_PROJECT_ID="your_firebase_project_id"

# ── Firebase (Authentication) ─────────────────────────────
# Required for Firebase Auth. Get these from Firebase Console -> Project Settings
VITE_FIREBASE_API_KEY="your_api_key"
VITE_FIREBASE_AUTH_DOMAIN="your_project_id.firebaseapp.com"
VITE_FIREBASE_PROJECT_ID="your_project_id"
VITE_FIREBASE_STORAGE_BUCKET="your_project_id.firebasestorage.app"
VITE_FIREBASE_MESSAGING_SENDER_ID="your_messaging_sender_id"
VITE_FIREBASE_APP_ID="your_app_id"

# ── API proxy server ──────────────────────────────────────
# Port for the local Express proxy server (default: 3001).
# SERVER_PORT=3001
