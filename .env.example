# ── Gemini ────────────────────────────────────────────────
# Required for Gemini models. Get yours at https://aistudio.google.com/apikey
GEMINI_API_KEY="your_gemini_api_key"

# ── Claude via Vertex AI ──────────────────────────────────
# GCP project ID that has Vertex AI / Claude access enabled.
VERTEX_PROJECT_ID="your_gcp_project_id"

# Vertex AI region. Use "global" for the global endpoint (recommended).
VERTEX_REGION="global"

# ── API proxy server ──────────────────────────────────────
# Port for the local Express proxy server (default: 3001).
# SERVER_PORT=3001
